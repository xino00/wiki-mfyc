"use client";
import {FormEvent,useState} from "react";
export default function Login(){const [error,setError]=useState(""); async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setError("");const data=Object.fromEntries(new FormData(event.currentTarget));const response=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(data)});if(response.ok){location.href=new URLSearchParams(location.search).get("next")??"/editor"}else setError("Credenciales inválidas")}return <main><h1>Acceso editorial</h1><p>La consulta clínica pública no requiere una cuenta.</p><form className="search" onSubmit={submit}><input name="email" type="email" required placeholder="Correo editorial"/><input name="password" type="password" required minLength={12} placeholder="Contraseña"/><button>Entrar</button></form><p role="alert">{error}</p></main>}
